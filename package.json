{
  "name": "first-webgl2-sprite",
  "version": "1.0.0",
  "description": "Minimal raw WebGL2 textured quad renderer: GLSL ES 3.00 shader compilation, procedural checkerboard texture upload, vertex array objects, and clip-space math.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "bench": "vite-node src/bench-cli.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.9.0",
    "vite": "^6.0.0",
    "vite-node": "^2.1.0",
    "vitest": "^2.1.0"
  }
}
